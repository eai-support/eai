# Delivery Lineage

The Docs drawer provides local guide search and sends questions to the Enterprise AI website chat API. That API calls the scoped EAI Platform semantic index and returns cited public sources. No visitor credentials or customer content are stored in the static Docs site.
